import React from 'react'
import { CharacterCardProps } from '../types'

function CharacterCard({characters}: CharacterCardProps) {
  return (
    <div>CharacterCard</div>
  )
}

export default CharacterCard
